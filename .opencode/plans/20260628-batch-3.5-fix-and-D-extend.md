# 批 3.5 Bug 修复 + 批 4 D1-D10 远期扩展

> Plan 日期：2026-06-28 | 状态：执行中
> 由 GLM 5.2 规划，build agent 执行

---

## 前提：依赖核实结果（关键事实）

| # | 事实 | 影响 |
|---|------|------|
| 1 | shareController 仅以单参数 `(snapshot_id)` 调 `getSnapshotById`（`shareController.js:36`），这是公开分享路径 | F1 不能简单删除无 userId 版本，必须保留为 `getSnapshotByIdPublic` |
| 2 | `cosmos_snapshots` 表**无 `updated_at` 字段**（v5 定义 + v7 ALTER 都没补） | F1 改编辑接口走 UPDATE 必须先新增迁移加 `updated_at` |
| 3 | `multer` / `node-cron` 均未安装 | D1 文件上传需装 `multer`；D10 定时任务需装 `node-cron` |
| 4 | `aiProviderService` 仅 2 个顶层 export（`callAi` / `extractJson`） | D4 新增 `transcribeAudio` 可仿照写为第三个顶层 export |
| 5 | `aiController` 仅 1 个端点 `POST /api/ai/chat`，耦合了 notes/kb 查询 | D7 不复用 aiController，新建独立 `mindGalaxyGuideController.js` |
| 6 | `preprocessService.preprocess(userId, { sources, options })` 已是导入器可复用的入库入口 | D1-D4 导入器统一调此函数入库 |
| 7 | `notes` 表 `id` 为 `VARCHAR(100)`（非自增整数） | D1-D4 导入器需自行生成唯一 id |
| 8 | mind-galaxy.html 已有 importmap（`:11-18`），本地 JS 全部通过 ES module import 进入 | D5/D6 新增 aggregator 模块直接通过 import 链接，无需 HTML 加 `<script src>` |
| 9 | `nlpDeepService` 仅 1 个 export `analyzeDeep` | D7/D8/D9 扩展该服务需新增 export 函数 |
| 10 | DASHSCOPE_API_KEY 环境变量已存在 | D4 复用此密钥调 paraformer-v2 语音识别 |
| 11 | `getReport` 原代码是 `repo.getSnapshotById(snapshotId)`（单参，无 userId） | F1 改为 `repo.getSnapshotById(snapshotId, userId)`，参数从 `req.user.id` 取 |
| 12 | 编辑接口当前走 `saveSnapshot`（INSERT）→ 每次编辑都新增 `cosmos_snapshots` 行 | F1 新增 `updateSnapshotJson`（UPDATE）+ `migrations_v8.sql` 加 `updated_at` 列 |
| 13 | Node 18+ 内置 `fetch` + `FormData` 可直接 multipart 上传二进制 | D4 无需装 `form-data` 包 |

---

## 执行顺序

```
F1 → F3 → F2                (批3.5，3 张，跑 smoke test 通过后)
   ↓
D1 ∥ D2                     (导入基类并行启动)
   ↓
D3（串在 D1 后） ∥ D4（串在 D2 后）
   ↓
D5 → D6                     (多用户串行：D6 依赖 D5 关系基础设施)
   ↓
D7 → D8 → D9               (AI 链串行)
   ↓
D10                         (依赖 D7 + D9 完成后最后跑)
```

---

# 批 3.5 · Bug 修复（3 张）

## F1 · backend_critical_fix ✅

### 目标
修复后端 3 个 Critical/High 阻断级 Bug：服务器启动崩溃（exportService 重复 `const`）、跨用户越权泄露（repository 重复方法覆盖带 userId 版本）、分享 token 可预测（`Math.random`）+ 3 个 High 级 PDF/编辑持久化缺陷。

### 改文件
| 文件 | 改动 | 锚点行号 |
|------|------|---------|
| `server/services/mindGalaxy/exportService.js` | ① 删除第 184 行重复 `const beliefs`（复用第 166 行变量）；② `pageAdded` 监听移到 `doc.pipe(writable)` 后即时注册（原在第 462 行，在全部章节内容写完后才注册，导致除最后一页外所有页无页码）；③ 内容生成体包 `try/finally`，异常时 `doc.end()` | 166, 184, 108-111, 461-467 |
| `server/repositories/mindGalaxyRepository.js` | ① 第 90-99 行的 `getSnapshotById(snapshotId)` 重命名为 `getSnapshotByIdPublic(snapshotId)`，仅供 shareController 用（无 userId 的公开路径）；第 79-88 行的带 userId 版保留原名 `getSnapshotById(id, userId)`；② 新增 `updateSnapshotJson(id, userId, json)` 方法走 `UPDATE cosmos_snapshots SET snapshot_json=? WHERE id=? AND user_id=?` | 79-99 |
| `server/controllers/shareController.js` | ① `generateToken` 改用 `crypto.randomBytes(16).toString('base64url')` 替代 Math.random 循环拼接；② `shareController.js:36` 改调 `repo.getSnapshotByIdPublic(row.snapshot_id)`（匹配重命名后的方法） | 36, 45-51 |
| `server/controllers/mindGalaxyController.js` | ① `getReport`（`:205`）调用改为 `repo.getSnapshotById(snapshotId, userId)`（第二参从 `req.user.id` 取）；② `setPrivacySettings` localMode 分支先 `repo.getConfig(userId, '_privacy')` 读取旧值再合并（原直接覆盖，导致 afterDelete 设置丢失）；③ renameBody / classifyNoteToTopic / mergePersons / updatePersonIntimacy 全部改走 `repo.updateSnapshotJson(id, userId, json)` 不再 `saveSnapshot`（避免每次编辑 INSERT 新行污染时间轴） | 205, 230, 303-307, 354, 403, 419 |
| `server/db/migrations_v8.sql`（新增） | `ALTER TABLE cosmos_snapshots ADD COLUMN updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;` | 新建 |

### 数据契约
```js
// repository 方法变更
getSnapshotById(id, userId)                  // 保留，带 userId（v2 内部用，getReport 等）
getSnapshotByIdPublic(snapshotId)            // 重命名旧 getSnapshotById(snapshotId)，无 userId（shareController 用）
updateSnapshotJson(id, userId, json)         // 新增，UPDATE cosmos_snapshots SET snapshot_json=?
// 返回 boolean（true if affectedRows > 0）

// shareController.js generateToken 变更
crypto.randomBytes(16).toString('base64url')  // 替代 Math.random 循环拼接
// 输出长度 = 22 字符（16 字节 × 4/3 ≈ 22）
```

### 验收清单
1. `node server/index.js` 启动不抛 `SyntaxError: Identifier 'beliefs' has already been declared`
2. 跨用户调 `GET /api/mind-galaxy/report?snapshotId=<他人 id>` 返回 `{ success: false, error: '未找到报告' }`（404）
3. 访问 `/api/share/<token>`（任意公开分享链接）仍可正常加载快照数据
4. PDF 每页底部均有页码 `— N —`（pageAdded 监听注册顺序已修复）
5. PDF 生成中注入报错（如非法数据结构）→ 客户端 5s 内收到 connection close，不长时间挂起
6. 连续 5 次 `renameBody` 后 `SELECT COUNT(*) FROM cosmos_snapshots WHERE user_id=?` 行数不变（走 UPDATE）
7. `POST /api/mind-galaxy/privacy/after-delete` 开启后，再 `POST .../local-mode` 设为 `true` → `GET .../privacy/config` 同时返回 `afterDelete: true` 与 `localMode: true`
8. 新 token 满足 `crypto.randomBytes(16).toString('base64url').length === 22`，连续两条互相无前缀规律
9. `updateSnapshotJson` 返回 false 时 controller 抛出 404

### 禁区
- 不改前端任何文件
- 不改 SQL 表结构除新增 `updated_at` 一列
- 不动 `saveSnapshot` 已有签名和调用方（仅新增 `updateSnapshotJson`）
- 不引入第三方加密库（使用 Node 内置 `crypto`）
- 不改 `routes/mindGalaxy.js` 路由注册

### 边界情况
- `getSnapshotById(snapshotId, userId)` 被多处调用 → grep 全文件 `getSnapshotById(` 替换：`shareController.js:36` 改 `getSnapshotByIdPublic`，其余 `mindGalaxyController.js` 确认是否传了 userId
- `setPrivacySettings` 的 `afterDelete` 分支（`:309-311`）已有正确合并逻辑（先 getConfig 再 spread），本次只补 `localMode` 分支
- 编辑接口走 `updateSnapshotJson` 时若 snapshot 不存在 → 0 affected rows → controller 应返回 404
- `saveSnapshot` 内部有 `sun_render_type` 计算（第 55 行 `bodies.find(b => b.type === 'black_hole')`）→ `updateSnapshotJson` 不需要这些计算字段，只需更新 `snapshot_json` 和 `updated_at`
- 空 share token 传给 `generateToken` 的替代：`crypto.randomBytes(0)` → 不会发生（函数内直接调用）
- PDF 的 `pageNum` 初始值为 1（已移到 `doc.pipe` 后，cover 页计入页码）

---

## F3 · lifecycle_fix ✅

### 目标
修复过渡 RAF 不受 unmount 管理（页面卸载抛错）+ `__mgSnapshot` 两分支赋值不一致导致海报失效 + `bodyBaseStates` 不重建导致过渡后星体漂移 + SSE 未监听客户端断开。

### 改文件
| 文件 | 改动 | 锚点行号 |
|------|------|---------|
| `public/js/modules/mindGalaxy/index.js` | ① 新增模块级 `_transitionRAF` 保存过渡动画 id；② `transitionToSnapshot` 每帧 `if (!mounted) { cancelAnimationFrame(_transitionRAF); return; }`；③ 第 166 行非 smooth 分支统一 `window.__mgSnapshot = json`（原为 `window.__mgSnapshot = snap` 导致取 `bodies` 是 undefined）；④ 过渡 `t>=1` 后遍历 `celestialItems` 重建 `bodyBaseStates`；⑤ `unmountMindGalaxy` 同时 `cancelAnimationFrame(_transitionRAF)` | 90, 149-198, 202-291, 429 |
| `server/controllers/mindGalaxyController.js` | `analyzeGalaxyStream` 和 `generateV2Snapshot`：① `setupSSE(res)` 后立即 `let aborted = false; req.on('close', () => { aborted = true; });`；② 各 `await await` 阶段后 `if (aborted) return;`；③ 关键 LLM 调用（`:102` `analyzeDeep`）前再检查一次 | 79-128, 147-175 |

### 验收清单
1. 演化时间轴切换到中途（`transitionToSnapshot` 进行中）→ 点击浏览器返回 → 控制台无 `Cannot read properties of null`
2. 非 smooth 路径（`replaceWithSnapshot` 直接替换）→ 立刻点击「信念星图」海报导出 → 海报生成成功（不再 `bodies` undefined）
3. 过渡到新快照 `t>=1` 后等 5s → 星体在新的位置附近漂浮（不再漂回旧坐标）
4. curl SSE `POST /api/mind-galaxy/analyze-stream` 客户端中途 Ctrl+C → 服务端日志显示 `aborted, skip saveGraph`
5. SSE 已发送 `event: done` 后再断开连接不报错

### 禁区
- 不动 `celestialBodies*.js` 工厂函数
- 不修改主 `animate()` 循环结构（仅 cancel 新增的 `_transitionRAF`）
- SSE 改动不引入 buf，保持当前直接写 res 的模式

### 边界情况
- `mounted` 标志位必须在 `unmountMindGalaxy` 第一行置 false（避免 `cancelAnimationFrame` 与 step 竞争）
- `_transitionRAF` 在 `t>=1` 后清理 `_newItem` 引用 + 置 null
- 客户端断开后若 DB 已经开始写入（saveGraph 已开始），只能跳过后续阶段不能回滚 → 日志打印 `aborted-after-saveGraph`
- `bodyBaseStates` 重建时机：`t>=1 && !bodyBaseStates.has()` 才 set

---

## F2 · frontend_xss_interaction ✅

### 目标
修复前端 2 个 Critical 级渲染崩溃（`new (window.THREE)().Vector3()` 导致双击/搜索聚焦全失效）+ 5 处 innerHTML XSS + 3 处资源/竞态缺陷。

### 改文件
| 文件 | 改动 | 锚点行号 |
|------|------|---------|
| `public/js/modules/mindGalaxy/interaction.js` | ① 第 106, 127 行 `new (window.THREE)().Vector3(...)` 改为 `new window.THREE.Vector3(...)`；② `updateDetailPanel` 中所有 `data.*`/`meta.*` 字段统一调 `escapeHtml()`；③ `initSearch` 监听引用存入 `_searchListeners` 数组，`disposeInteraction` 中 `removeEventListener`；④ 删除第 257-266 行 `querySelectorAll` 死代码（canvas 场景无 DOM 属性）；⑤ `onDoubleClick` 重新 raycast 取 `selectedObj` | 106, 127, 205-222, 257-266, 318-352, 104-109 |
| `public/js/modules/cosmos.js` | `openDetailPanel` 中 `data.label`、`typeNames[data.type]`、`Object.entries(data.meta)` 的 `v` 全部过 `escapeHtml()` | 344-358 |
| `public/js/modules/mindGalaxy/uiPanels.js` | `renderPersonPanel` 的 `p.name`、`renderReportPanel` 的 `b.label`/`p.name` 统一调 `escapeHtml()`（该函数已在 `uiPanels.js:716` 存在） | 472, 650, 670 |
| `public/js/modules/mindGalaxy/exporter.js` | `exportVideo` 把 `_videoChunks` 改为 `MediaRecorder` 实例局部状态而非模块级共享 | 264-317 |

### 验收清单
1. 双击星体 → 相机聚焦距天体 5 单位（giant_star=8, nebula=15），过渡 ~1.2s easeOutQuad
2. 搜索栏输入关键字 → 首个匹配星体闪烁 3 次 + 相机滑至该星体
3. 重命名为 `<img src=x onerror="alert('xss')">` 后点击详情面板 → 不弹 alert
4. cosmos 视图中便签分类设为 `<script>alert(1)</script>` → 点击对应天体不执行脚本
5. SPA 内 mount→unmount→mount 3 次 → 搜索 `test` 仅触发 1 次 lookup
6. 视频录制中再点击「视频」→ 立即停止录制并产出第一次 WebM blob（无 truncation）

### 禁区
- 不动后端文件
- 不改 `escapeHtml` 已有实现（`uiPanels.js:716`，只使用不修改）
- 不改 mount/unmount 入口签名

### 边界情况
- `escapeHtml(null)` / `escapeHtml(undefined)` 必须返回 `''` 而非 `'null'`
- `data.coreBelief` 可能含换行 → `escapeHtml` 保留 `\n` 转为 `<br>` 或 CSS `white-space: pre-wrap`
- 重命名后详情面板 DOM 重建时事件监听不丢 → 使用事件委托而非 attach
- 连续两次 `exportVideo` 快速点击 → 第二次应正常录制，不影响第一次

---

# 批 4 · D1-D10 远期扩展（10 张）

## D1 · import_dayone

### 目标
支持 Day One 导出的 JSON 文件导入，含 location/weather/tags/photos 元数据完整迁移为便签系统内的日记。

### 改文件
- **新增** `server/services/import/dayoneImport.js` — DayOne JSON 解析器
- **新增** `server/controllers/importController.js` — 通用导入控制器（D1-D4 共用）
- **新增** `server/routes/import.js` — 导入路由（D1-D4 共用），挂载于 `/api/import`
- **改** `server/routes/api.js` — 第 51 行 `router.use('/mind-galaxy', ...)` 后挂 `router.use('/import', importRouter)`
- **改** `server/package.json` — 新增 `multer` 依赖
- **改** `server/middleware/auth.js` — 确认已有 `authMiddleware` 可复用

### 数据契约
```js
// dayoneImport.js
async function parseDayOneJson(fileBuffer) → {
  entries: Array<{
    uuid: string,                           // DayOne 原始 uuid，用于幂等去重
    sampledAt: string,                       // ISO 8601，保留原时区
    content: string,                          // 纯文本正文
    title: string,                             // 取正文首行或前 30 字符
    meta: {
      tags?: string[],
      location?: { lat: number, lng: number, placeName: string },
      weather?: { tempF: number, conditions: string, iconName: string },
      photos?: string[]                        // 文件名列表（不下载二进制）
    }
  }>,
  skipped: number
}

// importController.js
POST /api/import/dayone (multipart, field=file) →
  await parseDayOneJson(req.file.buffer)
  for each entry:
    if (noteRepo.findByUuid(entry.uuid, userId)) skipped++
    else:
      noteRepo.create({
        id: `do_${entry.uuid.slice(0, 28)}`, user_id, title, content,
        category: 'DayOne导入',
        meta_json: { dayone: { weather: entry.meta.weather, location: entry.meta.location, photos: entry.meta.photos, sampledAt: entry.sampledAt } }
      })
      preprocessService.preprocess(userId, { sources: [{ type: 'notes', text: entry.content, ref: noteId, timestamp: entry.sampledAt }] })
  return success({ imported, skipped })
```

### 验收清单
1. 上传含 100 条记录的 Day One JSON → 写入 100 条 `notes` + 100 条 `data_sources`
2. weather/location 正确写入 `notes.meta_json.dayone.weather` / `.location`
3. 重复上传相同文件 → `{ imported: 0, skipped: 100, success: true }`，无重复 notes 行
4. 单条 content 超 50KB → 截断 + `meta_json.dayone.truncated = true`
5. 文件 > 10MB → multer limit 拒绝返回 413
6. JSON 格式错误 / 缺 `entries` 字段 → 400 + `error: 'invalid dayone format'`

### 禁区
- 不改 `preprocessService.js` 内部实现
- 不下载图片二进制（仅存文件名占位）
- 不动 `noteController` 已有 CRUD 端点
- 导入的 note `category` 固定为 `'DayOne导入'`

### 边界情况
- DayOne 导出可能是 `{ entries: [...] }` 或裸数组 `[...]` → parser 兼容两种
- `uuid` 为 null → 用 `hash(content + sampledAt)` 截 32 字符生成伪 uuid，日志 warn
- 时区 `2023-04-01T08:00:00+08:00` 直接保留为 ISO 字符串，不做转换
- 照片字段只存文件名 `photo_xxx.jpg`，前端显示「图片未迁移」占位

---

## D2 · import_notion_obsidian_etc

### 目标
统一 `BaseImporter` 抽象类，接 Notion（zip 导出）/ Obsidian（markdown 目录）/ 印象笔记（enex）/ 飞书（docx）4 种格式入库。

### 改文件
- **新增** `server/services/import/baseImporter.js` — 抽象基类
- **新增** `server/services/import/notionImport.js` — Notion zip 解压
- **新增** `server/services/import/obsidianImport.js` — Obsidian md 目录
- **新增** `server/services/import/evernoteImport.js` — enex XML
- **新增** `server/services/import/feishuImport.js` — docx zip
- **改** `server/controllers/importController.js` — 增 4 端点
- **改** `server/package.json` — 视需要加 `adm-zip`（如 Node 内置不够）

### 数据契约
```js
class BaseImporter {
  static canHandle(filename, mimeType) → boolean
  async parse(buffer, opts) → { entries: Note[], skipped: number }
}

// Notion: zip 解压 → 部分是 .md，部分是 sub-*.zip 子页面
// → 递归解析每个 .md，front-matter 提取元数据
// → 子 zip 递归调用自身 parse

// Obsidian: 目录下 .md 文件
// → `[[wiki link]]` 抽取为 tags
// → front-matter `---` 轻量解析（不引 yaml 库）

// enex: XML 解析 <note> 节点
// → <title>/<content>(HTML)/<created>(ISO)/<tag>
// → HTML 用轻量正则去标签得纯文本
// → <en-media> 替换为 `[图片未迁移]` 占位

// 飞书: docx zip → word/document.xml
// → 提取 <w:t> 文本，表格内容以 \n 连接
```

### 验收清单
1. 4 种格式各上传 1 个示例文件 → 全部入库，每条 note category 分别标记为 `'Notion导入'` / `'Obsidian导入'` / `'印象笔记导入'` / `'飞书导入'`
2. Notion zip 内含 3 层嵌套子页面 → 全部递归解析入库，父子关系记入 `meta_json.parentUuid`
3. Obsidian `[[Topic A]]` 抽取为 tag `Topic A`
4. enex `<en-media type="image/png">` → 正文插入 `[图片未迁移]` 占位
5. 飞书 docx 多段落表格 → 全文以 `\n` 连接，表格结构丢失但内容完整
6. 各格式 ≤ 10MB（multer limit）

### 禁区
- 不引 `mammoth`/`jszip` 等重量包；优先 Node 18+ 内置能力 + 少量正则
- 不引入第三方 YAML 解析器；Obsidian front-matter 只支持简单 key:value
- 不下载图片二进制

### 边界情况
- Notion 子页面 zip 内嵌套 zip → 递归调用 parse
- Obsidian 文件无 front-matter → 跳过元数据解析，返回空对象
- enex 时间格式 `YYYYMMDDTHHMMSSZ` → 转标准 ISO
- 飞书 docx 加密版本 → 解压失败返回 400 + `error: 'encrypted docx not supported'`
- 空目录/空 zip → 返回 `{ imported: 0, success: true }`

---

## D3 · import_chatlog

### 目标
微信聊天记录 txt 导入，按说话人切分、30 分钟聚合为「对话日记」入库。

### 改文件
- **新增** `server/services/import/chatlogImport.js`
- **改** `server/controllers/importController.js` — 增 `POST /api/import/chatlog`

### 数据契约
```js
// chatlogImport.js
const LINE_RE = /^(\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}) (.+?)$\n(.+)$/gm;

async function parseChatlog(text, userId) → {
  sessions: Array<{
    startTime: string,
    endTime: string,
    content: string,
    title: string,
    participants: string[]
  }>
}

// 30 分钟窗口聚合规则：
// 连续时间间隔 < 30 分钟的归为同一段
// 每段产出 session 对象写入 notes
// note.id = `wg_${uuid.slice(0,28)}`, category='微信导入'
```

### 验收清单
1. 上传 500 条 txt → 生成 ~20 篇聚合 session（视消息密度）
2. 每篇 `meta_json.participants` = 出现的说话人列表（去重）
3. 系统消息格式 `---- xxx 撤回了一条消息 ----` / `-- 以上是安全提醒 --` 等被过滤
4. 无时间戳的多行消息归并到前一段
5. 单 session 超 50KB 截断 + `meta.truncated = true`

### 禁区
- 不识别微信特定表情编码（`[微笑]` 保持原文本）
- 不解析语音/图片消息特殊标记
- 不存说话人头像

### 边界情况
- 跨午夜对话（23:50-00:30）→ 视为同一段，startTime 用首行
- 同一说话人连续发言 → 全部保留不合并
- 群聊 `<张三 在群里@李四>` → 说话人解析为 `张三`，被@的记入 `meta.mentioned`
- 无有效消息行 → 返回 `{ imported: 0, message: '未识别有效聊天记录' }`

---

## D4 · import_voice

### 目标
语音文件转写为文字 + 情感标签入库，复用现有 DASHSCOPE_API_KEY 调用 paraformer-v2。

### 改文件
- **新增** `server/services/import/voiceImport.js`
- **改** `server/services/aiProviderService.js` — 新增 `export async function transcribeAudio(opts)`
- **改** `server/controllers/importController.js` — 增 `POST /api/import/voice`
- **改** `server/config/index.js` — dashscope 段新增 `audioEndpoint` 配置（复用已有 DASHSCOPE_API_KEY）

### 数据契约
```js
// aiProviderService.js
export async function transcribeAudio({ audioBuffer, format, language = 'zh' }) → {
  text: string,
  sentences: Array<{
    text: string,
    beginTime: number,
    endTime: number,
    emotion?: string   // paraformer-v2 内置 emotion tag
  }>
}

// 调用 DashScope API:
// POST https://dashscope.aliyuncs.com/api/v1/services/audio/asr/transcription
// Headers: Authorization: Bearer ${DASHSCOPE_API_KEY}
// Body: FormData { model: 'paraformer-v2', file: <Blob>, format, language }

// voiceImport.js
async function parseVoice(fileBuffer, userId, meta) → {
  segments: Array<{ text, sampledAt: now, emotion, duration }>
}
// note.id = `voice_${uuid().slice(0,28)}`, category='语音导入'
// meta_json.voice = { duration, emotion, audioFormat }
```

### 验收清单
1. 上传 1 分钟 m4a → 转写为 ≥5 句段落，每句写为 1 条 data_sources
2. 每段 `emotion` 写入 `meta_json.voice.emotion`
3. DashScope 401/网络失败 → 返回 502 + `error: '语音识别服务不可用'` + 上游错误明细
4. 单文件 > 25MB → multer limit 拒绝返回 413
5. 支持 `m4a` / `mp3` / `wav` 三种格式
6. 转写完成后原始 audio buffer 不入库不存盘

### 禁区
- 不存音频原文到 localStorage 或 DB
- 不引 `form-data` 包（Node 18+ 内置 FormData 足够）
- 不调 Whisper 或第三方音频服务（统一 DashScope paraformer-v2）
- 不修改已有 `callAi` 函数签名

### 边界情况
- 静音音轨 → 返回 `{ imported: 0, message: '未识别出语音内容' }`
- paraformer-v2 不返回 emotion 字段 → `meta_json.voice.emotion = 'unknown'`
- 超慢速说话导致 multi-sentence → 用 `beginTime` 切分不合并
- 文件格式不支持 → 400 + `error: '不支持的文件格式，仅支持 m4a/mp3/wav'`

---

## D5 · multiplayer_relation

### 目标
两个用户的星系引力互动 + 共同关系节点高亮可视化，双方各自同意生成共享只读 token。

### 改文件
- **新增** `server/services/mindGalaxy/relationshipService.js`
- **新增** `server/db/migrations_v9.sql` — `relationship_invitations` 表
- **改** `server/controllers/mindGalaxyController.js` — 增 5 个端点
- **改** `server/routes/mindGalaxy.js` — 挂新路由
- **新增** `public/js/modules/mindGalaxy/relationship.js` — 前端双核螺旋渲染
- **改** `public/js/modules/mindGalaxy/index.js` — 启动时检测 `?rel=<token>` 加载关系模式

### 数据契约
```sql
CREATE TABLE relationship_invitations (
  id INT AUTO_INCREMENT PRIMARY KEY,
  inviter_id INT NOT NULL,
  invitee_id INT NOT NULL,
  status ENUM('pending','accepted','revoked') DEFAULT 'pending',
  share_token VARCHAR(32) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  accepted_at TIMESTAMP NULL,
  INDEX (inviter_id), INDEX (invitee_id), INDEX (share_token)
);
```

```js
// 端点
POST /api/mind-galaxy/relationship/invite          { inviteeId }   → { invitationId, shareToken }
POST /api/mind-galaxy/relationship/accept/:invId                    → { shareToken, ok }
POST /api/mind-galaxy/relationship/revoke/:invId                    → { ok }
GET  /api/mind-galaxy/relationship/list                             → [{ partnerId, status, token, createdAt }]
GET  /api/mind-galaxy/relationship/graph/:token                      → { cores: [{userId, snapshot}], bridge: [{name, intimacy}], edges: [{a, b, weight}] }

// relationshipService.js
buildRelationshipGalaxy(token) →
  // 取双方最新 snapshot.snapshot_json.bodies
  // 人物节点名字求交集 → bridge
  // 双方 galaxy 分置为双核螺旋，桥节点居中高亮金色
```

### 验收清单
1. A 邀请 → B 接受 → 双方获得 shareToken
2. 访问 `mind-galaxy.html?rel=<token>` 显示双核螺旋 + 共同节点金色高亮
3. 任一方 revoke → token 失效返回 410
4. 单人模式（不带 `?rel=`）不暴露此功能，无 UI
5. 双方最近 30 天无共同人物 → bridge 为空数组，提示「无共同点」

### 禁区
- 不存对方原始 notes
- 关系快照只走 `getSnapshotByIdPublic`（F1 重命名后的方法）
- 不允许任一方读对方原始 notes
- invitation 表只存邀请关系，不存便签数据

### 边界情况
- 邀请自己 → 400 + `error: '不能与自己建立关系'`
- 已 accept 后再次 accept → 410 + `error: '邀请已处理'`
- 双方一方删除全部 notes → bridge 重新计算，可能为空
- token 被猜中 → 概率 2^128，可忽略

---

## D6 · multiplayer_group

### 目标
3+ 用户匿名化聚合星系，**强制客户端本地匿名化 + 服务端不接收原文**，只接收匿名化的统计中间结果。

### 改文件
- **新增** `public/js/modules/mindGalaxy/aggregator.js` — 前端匿名化
- **新增** `server/services/mindGalaxy/aggregateService.js` — 服务端纯统计聚合
- **新增** `server/db/migrations_v10.sql` — `aggregate_sessions` 表
- **改** `server/config/index.js` — 新增 `aggregate.enabled = false`（默认关闭）
- **改** `server/controllers/mindGalaxyController.js` — 增 2 个端点
- **改** `server/routes/mindGalaxy.js` — 挂 `/aggregate/*`

### 强制匿名化规则
```js
// 前端匿名化步骤（aggregator.js）
function anonymize(snapshot) {
  // 1) 人物节点：用 personA/personB/... 替换（同一人的映射稳定）
  // 2) 地名：正则去 `中国`/`北京`/`地铁X号线` → [LOCATION]
  // 3) 时间戳精度降到周（2023-04-01T08:23 → 2023-W13）
  // 4) 主题向量：K-Means 后仅保留 cluster id + 中心点赋值
  // 5) 情绪直方图：8 维度分桶（焦虑/平静/忧郁/兴奋/...）
  return { anonymousPayload, topicVectors, emotionHistogram }
}
```

### 数据契约
```js
POST /api/mind-galaxy/aggregate/join { sessionId, anonymousPayload } →
  // 服务端只做纯统计聚合（不调 LLM）
  // topicVectors: 求 L2 平均
  // emotionHistogram: 各分桶累加
  // anonBodies: 仅统计 person 出现频次，不存姓名

GET /api/mind-galaxy/aggregate/result?sessionId= →
  { groupGalaxy, participantCount }
```

### 验收清单
1. 5 用户加入同一 session → 生成群体星系，**所有可读字段均为匿名**
2. 客户端日志全文 grep 真实姓名 → 0 命中
3. 群体星系前端不可点击查看原文（移除 click 事件）
4. `aggregate.enabled = false` → `/aggregate/*` 返回 403
5. 单个用户加入多个 session → 数据隔离，不跨 session 混合
6. 服务端 schema 严格校验：除 `topicVectors` / `emotionHistogram` / `anonBodies` 之外的字段一律拒绝

### 禁区
- 服务端不接受原文，schema 严格校验
- 不调 LLM 处理匿名 payload（只做 map-reduce 聚合）
- 群体星系不提供 sourceRef 点击
- 不允许跨 session 数据混合

### 边界情况
- 单人 session（participantCount=1）→ 允许
- 客户端 K-Means 不收敛 → 降级用 TF-IDF 中心点上传，日志 warn
- NER 未脱敏 → 客户端 hard fail，拒绝上传
- 服务器累计 payload 超 10MB → 限流

---

## D7 · ai_galaxy_guide

### 目标
自然语言对话式星系向导：「我最近的焦虑和谁有关？」→ 高亮节点 + 文字解读（流式 SSE）。

### 改文件
- **新增** `server/services/mindGalaxy/galaxyGuideService.js`
- **新增** `server/controllers/mindGalaxyGuideController.js` — 独立 controller，不复用 aiController
- **改** `server/routes/mindGalaxy.js` — 挂 `POST /api/mind-galaxy/ai-guide`
- **改** `server/services/mindGalaxy/nlpDeepService.js` — 增 `export function buildGalaxyContextPrompt(snapshot)`
- **改** `public/js/modules/mindGalaxy/index.js` — 注册 SSE 回调处理 action event

### 数据契约
```js
// galaxyGuideService.js
async function guideStream(userId, question, { onChunk, signal }) → {
  // 1) 取最新 snapshot
  // 2) buildGalaxyContextPrompt(snapshot) → ≤200 字摘要（星系类型 + top5 信念 + top5 人物）
  // 3) 拼 prompt: system + context + question
  // 4) 调 aiProviderService.callAi({ stream: true, onChunk })
  // 5) onChunk 解析 JSON `{ bodyId, action: 'highlight' | 'focus' | 'timeline' }` → sendSSE event: 'action'
  // 6) 剩余 chunk 文本 → sendSSE event: 'text'
}

// SSE events: 'text' | 'action:{bodyId,action,params}' | 'done' | 'error'
```

### 验收清单
1. 「最近焦虑」 → 流式 text + ≥1 个 `{ action: 'highlight', bodyId: <焦虑相关 body> }`
2. 「我和张三关系演化」 → `{ action: 'timeline', params: { targetTime: <首次共现时间> } }`，前端跳转时间轴
3. 「整体评估」 → 纯 text，无 action
4. localMode 开启 → 走前端兜底：本地 NLP 抽取关键词高亮 + 模板回复
5. 服务不可用（401） → SSE `event: error`

### 禁区
- 不复用 `aiController.chatStream`
- context prompt ≤ 300 字
- 不上传完整 snapshot，仅上传摘要

### 边界情况
- snapshot 不存在 → 404 + `error: '请先生成星系快照'`
- 问题 > 500 字 → 截断并提示
- 用户输入纯命令 `/focus black_hole` → 直接返回 action 不调 LLM

---

## D8 · ai_socratic

### 目标
苏格拉底引导式探索：澄清 → 反例 → 求证 → 总结 4 阶段，AI 只提问不给建议。

### 改文件
- **新增** `server/services/mindGalaxy/socraticService.js` + 4 prompt 模板
- **新增** `public/js/modules/mindGalaxy/socratic.js` — 前端会话状态机
- **改** `mind-galaxy.html` — socratic 模式入口按钮
- **新增** `server/db/migrations_v11.sql` — `socratic_sessions` 表

### 数据契约
```js
// socraticService.js
async function socraticStep({ sessionId, userId, userUtterance }) → {
  stage: 'clarify' | 'counterexample' | 'verify' | 'summary',
  aiUtterance: string,
  doneTurns: number,
  isNewStage: boolean
}
// 状态机：start → clarify (3turns) → counterexample (2turns) → verify (2turns) → summary
// summary 自动收尾输出「我注意到的三个模式」
```

### 验收清单
1. 「我决定辞职」→ 第 1 轮 3 个开放性提问（无建议无判断）
2. counterexample 阶段 → 「假设...会怎样？」
3. summary → 格式化「1)... 2)... 3)...」
4. 每 5 轮自动 summary
5. 历史持久化到 `socratic_sessions`
6. AI 输出含「你应该」「建议你」→ 后处理替换为开放问题模板

### 禁区
- 不引入判断性话术
- 不调 RAG 检索用户便签
- AI 不得编造用户过去内容

### 边界情况
- 连续 3 轮沉默 → AI 跳到 summary
- prompt 模板失败 → 前端 toast「已保存当前进度」
- 同一用户单 session 限制（防止状态机混乱）

---

## D9 · ai_belief_check

### 目标
针对单条 coreBelief 做 5 维度风险评估 + ≥2 条替代信念建议。

### 改文件
- **新增** `server/services/mindGalaxy/beliefCheckService.js`
- **新增** `server/db/migrations_v12.sql` — `belief_checks` 表
- **改** `server/services/mindGalaxy/nlpDeepService.js` — 增 `analyzeDeepBelief(userId, belief)`
- **改** `public/js/modules/mindGalaxy/uiPanels.js` — 报告面板增「检验信念」按钮

### 数据契约
```js
// beliefCheckService.js
async function checkBelief(userId, beliefText) → {
  scores: {
    evidenceStrength: 0..1,        // 证据强度
    logicalConsistency: 0..1,       // 逻辑自洽
    counterexampleTolerance: 0..1,  // 反例容纳
    emotionalLoad: 0..1,            // 情绪负载
    behavioralConsequence: 0..1     // 行为后果
  },
  risk: 'low' | 'medium' | 'high',
  alternatives: string[],   // ≥2 条
  rawAnalysis: string
}
// risk = 'high' if avg(scores) < 0.4
// risk = 'low'  if avg(scores) > 0.7
// risk = 'medium' otherwise
```

### 验收清单
1. 选中「我不是好父母」→ 输出 5 分数 + risk + ≥2 条替代
2. risk = 'high' → 该 belief 旁加红色三角警示
3. 历史检验持久化到 `belief_checks`（含 rawAnalysis）
4. 同一 belief 多次检验 → 返回最新 + 历史列表
5. LLM 失败 → 降级规则评分（基于关键词「我不」「永远」「绝不」+ 高频情绪词）

### 禁区
- 不存原便签内容到 `belief_checks`
- 不允许「删除信念」行为（只允许补充替代）

### 边界情况
- belief 文本为空 → 400
- belief > 200 字 → 截断 + meta.truncated
- 5 维全为 0（LLM 解析失败）→ 后端补 default 0.5

---

## D10 · digital_twin_evolve

### 目标
数字心智持续演化 + 跨时空对话（与过去的自己对话），node-cron 每日 03:00。

### 改文件
- **新增** `server/services/mindGalaxy/digitalTwinService.js`
- **改** `server/index.js` — 启动时调用 `startDigitalTwinCron()`，`_cronStarted` 标志防重复注册
- **改** `server/config/index.js` — 新增 `digitalTwin.enabled = true` + `digitalTwin.cron = '0 3 * * *'`
- **新增** `server/db/migrations_v13.sql` — `digital_twin_snapshots` 表
- **改** `server/package.json` — 新增 `node-cron` 依赖

### 数据契约
```sql
CREATE TABLE digital_twin_snapshots (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  epoch DATE NOT NULL,
  epoch_end DATE NOT NULL,
  persona_json JSON NOT NULL,
  source_snapshot_id INT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE (user_id, epoch)
);
```

```js
// digitalTwinService.js
async function evolveDigitalTwin(userId) →
  // 1) 查最近 7 天 data_sources（createdAt > 上次 epoch_end）
  // 2) 跑 preprocess + nlpBasic + nlpDeep → belief/emotion/topic 摘要
  // 3) 写入 digital_twin_snapshots，epoch = 当前周周一

async function chatWithPastSelf(userId, epoch, message) →
  // 1) 取 userId + epoch 的 persona_json 作 system prompt
  // 2) 调 callAi stream
  // 3) system prompt: 「你是 <username> 在 <epoch> 时期的内心状态」
```

### 验收清单
1. 连续 7 天每天 1 篇 → 第 8 天 03:00 cron 跑出衍生 digital_twin_snapshots
2. 1 月 vs 6 月跨时空对话风格显著差异
3. pm2 restart 不重复注册 cron（`_cronStarted` 检测）
4. `digitalTwin.enabled = false` → cron 不启动
5. 7 天无新 data_sources → 日志 `skip user, no new data`

### 禁区
- 不允许「删除人格」操作
- 不在前端持久化对话历史
- cron 失败单用户隔离，不阻断其他用户

### 边界情况
- 用户首次跑 cron 但无 data_sources → 不创建
- persona_json > 100KB → 压缩至 top-3 信念 + top-5 情绪倾向
- 对话超 30 分钟无活动 → 销毁上下文
- 跨年 epoch → 自然按 DATE 周边界

---

## 关键决策

| 决策 | 理由 |
|------|------|
| `getSnapshotById` 保留双版本 | share 公开路径需无 userId 版本，改名 `getSnapshotByIdPublic` 避免覆盖 |
| 新增 `migrations_v8.sql`（ADD updated_at） | `cosmos_snapshots` 无此字段，编辑接口 UPDATE 必需 |
| 新增 `migrations_v9-v13.sql`（每张一张） | D5-D10 各需新表，独立迁移可回滚 |
| 装 `multer` + `node-cron` | D1 上传 / D10 定时任务必需 |
| D4 不装 `form-data` | Node 18+ 内置 FormData 足够 |
| D7 不复用 aiController | 避免 RAG 耦合到 galaxy guide，解耦 |
| D4 复用 DashScope paraformer-v2 | 已有密钥，不引入新服务 |
| D5/D6 强制匿名化+opt-in | 隐私合规，默认关闭 |
| D10 基于 node-cron + `_cronStarted` | pm2 常驻进程支持，重启防重复 |

---

## 最终核验

完成全部后执行：
1. `node server/index.js` 启动无 SyntaxError
2. `node server/test/smoke.js` 全部通过
3. 手动：mind-galaxy.html 双击/搜索聚焦正常
4. 手动：重命名 XSS payload 不弹框
5. 手动：分享 token 不可预测
6. 手动：演化过渡中切换快照不报错
7. 手动：PDF 含全部页码
8. 手动：DayOne JSON → notes 入库
9. 手动：语音 m4a → 转写段落入库
10. 手动：双人关系双核螺旋视图
11. 手动：苏格拉底只提问不给建议
12. 手动：数字心智跨时空对话风格差异
